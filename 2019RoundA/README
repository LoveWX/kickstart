Problem A. 

Problem B. Parcels
平面中一些格点是快递站，其它是空地。
每个空地到最近的快递站的距离为该空地的分距离。这些分距离的最大值为全局距离。
求如果最多添加一个快递站，全局距离的最小是多少。
可以通过BFS求得每个空地到快递站的距离。
设全局距离最小值为k，则表示所有的分距离<=k。
对于大于k的值k1，也满足分距离<=k1；而对于0<=k2<k，至少有一个分距离>k2。
因此可以使用二分查找来找到全局距离的最小值k，它是最小的使得各个分距离<=k的值。
借由曼哈顿距离转切比雪夫距离的方法:
|x|+|y|=max(|x+y|,|x-y|)
两点(x1,y1)(x2,y2)之间的距离可以表示为：
 dist((x1,y1),(x2,y2))
=|x1-x2|+|y1-y2|
=max(|x1-x2+y1-y2|,|x1-x2-y1+y2|)
=max(|(x1+y1)-(x2+y2)|,|(x1-y1)-(x2-y2)|)
对于每个可能的新增快递站位置(x2,y2)，(x2+y2)和(x2-y2)是定值
而当空地(x1,y1)到(x2,y2)的距离取最大值时，
要么(x1+y1)取最大值；要么(x1+y1)取最小值；
要么(x1-y1)取最大值；要么(x1-y1)取最小值。
因此需要对每个分距离大于k的空地计算(x1+y1)和(x1-y1)。
所有空地的全局距离的最大值即：
max(|(x1+y1)max-(x2+y2)|,|(x1+y1)min-(x2+y2)|,|(x1-y1)max-(x2-y2)|,|(x1-y1)min-(x2-y2)|)   (1)
只要有空地(x2,y2)使得(1)式小于等于k，即表示全局距离为k是可行的。
因此对于每个固定的k，需要O(RC)的时间计算(1)式的值，需要O(RC)的时间检查是否存在空地使得所有分距离<=k
加上二分查找需要的O(log(RC))，总计需要O(RClog(RC))的时间

Problem C. 
