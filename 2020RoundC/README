Problem A. Countdown
形如K,K-1,...,2,1的数组叫做K-倒计时数组。求给定数组中K-倒计时数组的个数
直接遍历即可

Problem B. Stable Wall
一些不规则的拼图块组成了一个矩形。如果可以通过一种顺序搭建拼图使得每个新增的拼图都不悬空，就说明拼图是稳定的。
所谓不悬空，是指单个格点的下方，都是底线或者是已经填充的其它格点。
对给定的拼图求一个搭建序列使得拼图是稳定的。
可以将拼图看作有最多26个结点的图，单向边(u,v)表示需要先搭建u再搭建v
对于每一列，从下到上，如果相邻的两个格点不一样，则可看作从下方结点到上方结点有一单向边。
整个图的拓扑序即为搭建顺序。如果有环则说明无法搭建

Problem C. Perfect Subarray



Problem D. Candies
