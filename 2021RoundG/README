Problem A. Dogs and Cats
队列中有C只猫和D条狗，有一些猫粮和狗粮，每只猫吃一份猫粮。每条狗吃一份狗粮，但同时会添加M份猫粮。
求是否可以使得所有的狗都能吃到食物
模拟即可，时间复杂度O(N)

Problem B. Staying Hydrated
平面上有一些边平行于坐标轴的矩形，在平面上找一个点A使得该点到所有矩形的最短的曼哈顿距离的和最小
设所求点为(x,y)，则曼哈顿距离和为：
D=|x-x0|+|y-y0|+|x-x1|+|y-y1|+...+|x-xn|+|y-yn|
 =|x-x0|+|x-x1|+...+|x-xn|+|y-y0|+|y-y1|+...+|y-yn|
可见总和距离可以在x轴和y轴上分别求取再加和。因此问题从二维变为一维：
坐标轴上有一些线段，求一个点A使得该点到所有线段的距离的和最小
这个题目和“坐标轴上有一些点，求一个点A使得该点到所有点的距离的和最小”类似，可以得出相似的结论：
点A一定可以在某个线段的端点上取到，即点A的值域一定包含某个线段的端点
不同之处在于，线段到点A的距离需要根据点A在线段的哪一侧区分处理。
可以先将点A置于最左边的端点，逐个端点向右移动，依次计算总和距离的最小值。
可以使用三个集合来表示线段：
sleft：表示点A左侧的线段集合，它们到点A的距离都按其右端点计算
s0：表示包含点A的线段集合，它们到点A的距离为0
sright：表示点A右侧的线段集合，它们到点A的距离都按其左端点计算
从左到右依次遍历每个端点，先用sleft和sright的数量以及该端点和上一端点的距离计算总和距离的变化值，
以求得当前端点的距离和；再根据当前端点更新sleft, s0, sright使得点A从上一端点“移动”到当前端点
分别在x轴和y轴上求得结果即可。时间复杂度为O(2NlogN)

Problem C. Banana Bunches
给定正数数组，求取其中最多两个不相连的子数组，使得其和等于给定值K，且其总长度最小
注意这个题目的数组长度为N<=5000，K<=10^6，而子数组的个数是O(N^2)级别的，两个子数组枚举是O(N^4)级别
因此需要用O(K)规模或者O(N^2)规模的表来限制解空间规模
可以用一个[1,10^6]的数组A作为这个表。每个元素A[k]=L表示和为k的一个子数组最小长度为L
从右到左，对第一个子数组的最右元素进行枚举，则其左侧可能的子数组有O(N)个，注意每次枚举这些子数组的最右元素是固定的
这时第二个子数组的数量是O(N^2)个，但是可以用O(K)规模的数组A来替代，因此可以用O(1)时间查到右侧和为特定值的子数组的长度
具体来说，从右到左遍历数组。对于某个元素A[i]，枚举其左侧的各个元素A[j]，对于子数组A[j,...,i]，
在数组A中检查和为K-Sum(A[j,...,i])的子数组的最小长度，用这两个数组的和更新最终答案
再对于A[i]右侧的每一个元素k，将子数组A[i,...,k]及其和更新到数组A中，以便下一次查找
这样累积进行N轮，每次向左查询需要O(N)时间，向右更新需要O(N)时间，总体时间复杂度为O(N^2)
注意这种枚举中并不在意两个子数组是否相连，相连的时候即可以视为只有一个子数组。

Problem D. Simple Polygon
给定整数N和A，在平面上绘制一个简单多边形，使得其端点数为N，面积为A/2
所谓简单多边形，是指平面上
1.封闭的
2.不自相交的，即每个边除了顶点不和其它边相交，一个顶点只连接两个边
3.相邻的边不组成平角
的多边形，按序输出任一多边形的顶点的坐标
边长为1的正方形面积为1，如果取其中3个点，该三角形面积为1/2。
将边长为1的正方形其中一边向一侧移动一个单位，就能得到一个面积为1的平行四边形，其实也是有两个面积为1/2的三角形组成。
因此可以用这种平行四边形和三角形作为基本图形来绘图。
首先按照皮克定理，Area=i+b/2−1，其中i为多边形内部的整点个数，b为多边形边上的整点个数，保证给定的N和A能组成多边形
如果N为偶数，则可以绘制一个高(y轴)为1，宽(x轴)为N/2的矩形，其边上的整点恰好为N个。
将这个矩形用平行于y轴的经过整点的线划分，就能得到一系列的正方形。
每隔一个整点将该整点向下(y轴负向)平移一个单位，就可以将原来的正方形变为平行四边形，而面积不变
从这种图形：
  × × × ×
  × × × ×  ...
变为这种图形：
  ×   × 
  × × × ×  ...
    ×   ×
如果N为奇数，可以在最右侧的平行于y轴的边CD的外侧，再添加一个点E，用边CE和边ED替换边CD即可。
这样就满足了端点数的条件。
此时多边形可能面积不足，那么需要扩展一下原图形，在基本不破坏原结构的情况下增加一下面积。
可以找到原图形中“外置”的一些三角形，通过平移一个端点拉长其中两条边以增加面积
如果N为奇数，多边形面积为(N-1)/2-1，
最后添加的那个“外置”三角形就可以用来进行面积增补，即最后增加的这个点E需要和点C点D在x轴上相距A-((N-1)/2-1)*2
如果N为偶数，多边形面积为(N-2)/2，则可以选择左上角的三角形作为“外置”三角形，即(0,1)(0,2)(1,1)，
可以将最“外侧”的端点(0,2)向上(y轴正向)拉伸(A-N+2)/2即可
注意N为奇数时，最后添加的“外置”的端点E不能与相邻的边组成平角，
而只有一种情况会组成平角：该“外置”三角形的面积为1，且选择了(x+1,0)与最右边(x,0)(x,1)组成三角形
这时相邻的(x-1,2)(x,1)和(x,1)(x+1,0)构成平角
因此此时可以将“外置”三角形的最右端点E设置在y=1上
